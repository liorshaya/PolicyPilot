# PolicyPilot worklog

One line per day (Document 7, Daily routine and tracking): date, day number, what was done, what slipped,
decisions taken, and the numbers copied from the CI job summary (line coverage per package, PIT score, batch
time, first-token time; `n/a` until the number exists). Gate proofs and rehearsal fix lists are added under the
day they belong to. Slipped items open the next morning.

Format of a line:

```
| date | day | done | slipped | decisions | coverage per package | PIT | batch | first token |
```

| Date | Day | Done | Slipped | Decisions | Coverage per package | PIT | Batch | First token |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-18 | pre-1 | Repository reorganised into a monorepo (`backend/`, `frontend/`, `fixtures/`, `docs/`); the delivered `docs/` and `fixtures/` zips extracted as canonical, older drafts removed (kept in the first commit); backend skeleton (Spring Boot 4.0.8, Spring AI 2.0.1, Flyway V1 with pgvector, the eleven packages plus `config` and `common`, layer sub-packages, JaCoCo gates, PIT profile, Dockerfile by digest, Maven wrapper 3.9.16); ArchUnit suite (module table, nothing depends on `web`, Spring AI only in `ai.adapter`, forbidden APIs, engine purity); context-loads tests for `openai`, `ollama`, `openai,cloud`; Flyway and pgvector integration test; health endpoint test; `DATABASE_URL` translation for Railway with unit tests; frontend skeleton (React 19.3, TypeScript 6.0 strict, Vite 8.3, TanStack Query, Vitest 5 with the Document 6 thresholds, Playwright 1.63, MSW, ESLint 10) with the static access gate, component tests and the gate e2e test; Docker Compose (db, backend, frontend, optional ollama with the pull script) and `make up`; CI with the eight stages pinned to commit SHAs, `eval.yml`, Dependabot, the PR template with the Definition of Done; Semgrep rules incl. test-without-assertion; hygiene scripts (fixture privacy, schema copies, generated client, quarantine count, job summary); `.gitignore`, `.env.example`, `CLAUDE.md`, `RUNBOOK.md` (Hebrew), root README placeholder. Verified locally: `./mvnw verify` green (32 unit, 11 integration), Vitest 5 tests, Playwright 2 tests, reference self-test `ALL OK`, generator no diff, Semgrep 0 findings, compose config valid. First CI run on GitHub: stages 1 to 5 and 8 green, stage 6 red at the Trivy gate (CVE-2026-65182, CVE-2026-65905, CVE-2026-68525: critical, in Tomcat 11.0.24 as managed by Spring Boot 4.0.8), and the same failure on the 7 pull requests Dependabot opened right after the push; fixed in pull request #8 (Tomcat pinned to 11.0.26, stage 6 now scans before it pushes, Dependabot limited to the documented version lines), Dependabot pull requests #1 to #7 closed. | Gate G0 passed on 2026-09-18 (proof below). | Folder names `backend/` and `frontend/` (documents said `api/`, `web/`; exports patched, living documents to follow); `config` and `common` added as cross-cutting packages; `ai.daily-token-budget` left unset (no value in the documents); `@eslint-react` replaces `eslint-plugin-react` (no ESLint 10 support); jsdom pinned to 29.1.1 for Node 24.12; PIT `failWhenNoMutations=false` until day 3; `tomcat.version` overridden to 11.0.26 until a Spring Boot 4.0.x release manages a fixed Tomcat; the repository is public, so branch protection needs no paid plan; Railway set up as config as code (`backend/railway.json`), its database as a plain `pgvector/pgvector:pg16` image service because Railway's pgvector templates run PostgreSQL 18, and the Dockerfile without BuildKit cache mounts, which Railway accepts only with a service-specific id. | config 100% line (unit + IT); other packages empty | n/a (no classes in `engine`, `rules` yet) | n/a | n/a |
| 2026-09-19 | 2 | Day 2 (`rules`) done three days early, in five stacked pull requests (#20 to #24): the DSL 1.0 as records in `rules.model`; `rules.json` with the Document 5 limits (1 MB, depth 32) and a strict hand-written mapping that refuses unknown properties on every object kind; `rules.validation` with the schema layer (networknt 3.0.7), the semantic layer, the structural layer, the four contexts, the quote normalizer and the case validator, behind `RuleSetValidator` and `CaseValidator`. Every `invalid-*` file fails in Java with the reference's code, severity and layer stop; every structural fixture and variant returns the reference's finding list in order; lending v1 is clean in PUBLISH; the 31 conformance and 18 evaluation rule sets have no errors. Document 3 re-exported with the new quote normalization and the pointer rule; `reference_check.py` updated to match, `ALL OK`, generator no diff. 380 unit tests. | Nothing | Owner decisions of 2026-09-19: quote normalization strips every punctuation (P*) and format (Cf) character and needs three characters after normalization; findings point at the most specific node (the reference's path is a prefix). `rules` split into `model`, `json` and `validation`; schema errors of a `oneOf`/`anyOf` reduced to the closest alternative; RE2J 1.8 pinned; the case validator ignores undeclared properties, as the reference does. Tests beyond the approved list (shape and limit cases, `ConstraintTest`, edge cases) came from the coverage and mutation gates. A string longer than Document 3's 2,000 characters is CASE_OUT_OF_RANGE (owner decision; Document 3 step 1, the reference and the case validator changed together). | `rules.model` 100% line, 100% branch; `rules.json` 100%, 100%; `rules.validation` 100%, 99.2%; config 100% line | 100% on `rules` (990 of 990); not a hard gate until day 3 | n/a | n/a |
| 2026-09-19 | 3 | Day 3 (`engine`) done four days early, in three pull requests (#26, #27, #28): `ExpressionEvaluator` on `BigDecimal` (exact sums and products, division and negative powers at 12 places HALF_EVEN, whole exponents); `CompiledRuleSet` (priority-then-id order, RE2J patterns compiled once); `RuleEngine` with Document 3's eight steps, the five evaluation errors with the partial trace, severity resolution, simulation that never touches its base; `DecisionJson` in the reference's shape with a canonical serialization. Java and the reference agree on C-01 to C-31, on `sample-decision.json`, on all 200 cases of `cases-expected.json` and its summary, and on the labeled cases of all 18 evaluation policies; the 200 cases are byte-identical twice. jqwik properties with a fixed seed (never throws, one step per rule, nothing after a terminal rule, the Spitzer formula, severity resolution, identical bytes, exact arithmetic); ArchUnit bans reading the time in `engine`; the JMH profile (`-Pbenchmark`); PIT is a hard gate. 508 unit tests. | Nothing | Owner decisions of 2026-09-19, documents first: the reference now computes as Document 3 says (its 40-digit context could round sums; no golden file changed); `pow` takes a whole exponent, a negative one is a division at 12 places, a fractional one or 0^0 is EVAL_NON_FINITE; expression depth above 8 function nodes is the new code EXPR_DEPTH (Document 3 table, reference, `invalid-EXPR_DEPTH.json`). Found while testing: RE2 refuses a repeat count above 1000, which the reference did not know; it now reports REGEX_INVALID as the Java validator does. The engine returns no version, timing or stored id: the API adds them on day 5. | `engine` 100% line, 100% branch; `rules.model` 100%, 100%; `rules.json` 100%, 100%; `rules.validation` 100%, 99.2% | 100% on `engine` and `rules` (1207 of 1207), hard gate | engine 3.8 µs per case (JMH, local; the API batch arrives on day 5) | n/a |
| 2026-09-19 | 4 | Day 4 (front door, `policy`, `demo`) done five days early, in PRs #29 to #36: Documents 2, 5, 6 and 7 changed first (#29); the error envelope with the 13 codes of Document 2, trace ids on every response and log line (Micrometer Tracing, Brave), ECS JSON logs with a redacting customizer, the seven day 4 security counters, the secrets validated at startup (#30); `POST /api/v1/auth/code` with a constant-time compare, the 20-failures-in-15-minutes lockout, the HMAC cookie renewed after an hour, deny-by-default Spring Security, CORS, the client header and Origin check (#31); Bucket4j limits on the API clock, the stream registry, body, upload and charset limits, JSON depth 32 (#32); the policy tables (V2), paste or upload with type by magic bytes, PDFBox in memory with the Document 5 refusals, normalization, the splitter and limits, sandbox-scoped reads (#33); the gate in the web app (#35); coverage gates measured where Document 6 says (#36); the seeded protected lending policy and fork on write (V3, #34). The API moved to `api.policypilot.liorshaya.com` (Railway custom domain, Namecheap CNAME, Vercel `VITE_API_BASE_URL` as a Config variable); the owner signed in on the cloud site. 799 unit, 98 integration, 18 Vitest and 4 Playwright tests. | Nothing | Owner decisions of 2026-09-19, documents first: every recommendation of the day 4 test list accepted (15 questions), among them the API on its own subdomain with `SameSite=Lax` (a cross-site cookie is third-party and Safari blocks it), the error code enum, `policy_document.protected` and later `forked_from_id`, normalization and upload reading in `web`, 40 KB as 40,960 bytes, PDFs with JavaScript, embedded files or encryption refused, the IP-hash key derived from the cookie secret. Found while working: stage 2 gates coverage on unit tests alone, and a local `verify -DskipITs` after a full run passes falsely because JaCoCo appends to its file (always `clean`); stage 2 now checks the unit-measured packages and stage 5 every package (owner decision, Document 6 first, no threshold changed). The Write tool turned `\uXXXX` escapes into real invisible characters in five test sources, and two older ones on `main` had them; all written as escapes again. A stage 4 run on `main` failed on npm's maintenance window and was re-run. The contract test found two real mismatches with the served OpenAPI document (200 for 201, a null `forkedFromId`), both fixed. | `engine` 100% line, 100% branch; `rules.*` 100%, 99.2% to 100%; `web.security` 98.8%, 95.5%; `web.validation` 97.7%, 93.6%; `web.error` 98.4%, 96.9%; `web.controller` 96.0%, 100%; `policy.service` 100%, 100%; `policy.entity` 97.9%; `demo.service` 90.3%, 83.3%; `common` 94.7%; `config` 95.5%, 77.8% | 100% on `engine` and `rules` (1207 of 1207), hard gate | n/a (the batch arrives on day 5) | n/a |
| 2026-09-20 | 5 | Day 5 (versions, decisions, audit) done seven days early, in four pull requests (#37 to #40): Documents 2, 5 and 6 changed first (#37); V4 with `ruleset`, `ruleset_version`, `rule` and `audit_entry`, the trigger that freezes a published version, and `policypilot_app`, the role every API connection switches to while Flyway keeps the owner's connection; the `ruleset` module (drafts, manual edits, publish in one transaction with its audit entry and the `rule` rows, the compiled rule set cached per version, fork on write of the protected rule set), the append-only `audit` module, and the routes `GET /rulesets`, `GET` a version, `PUT .../rules`, `POST .../publish` (#38); V5 with `case_fixture` and `decision`, the `decision` module (one case, a list of at most 500, the fixture set, statistics over the latest decision per case, simulation that stores nothing) and its routes, the batch limit of 5 per minute per sandbox, `CASE_INVALID` (#39); the JSON and CSV exports with formula prefixing, a byte order mark and the attachment disposition, and the day closed (#40); the decisions of a batch are written in JDBC batches (#41, after the cloud check). The seed now loads the rule set and the 200 cases as protected rows. Checked on the cloud site after the deploy: the 200 cases decide with aggregates equal to `cases-expected.json`. 826 unit, 192 integration, 18 Vitest and 4 Playwright tests. | Nothing | Owner decisions of 2026-09-20 (the twelve open questions of the day 5 test list were delegated to Claude and are recorded in Documents 2, 5 and 6): a twelfth package, `ruleset`, because publishing needs `engine`, `audit` and `policy` together and `decision` must read versions; the API runs as a restricted role reached with `SET ROLE`, so the grants hold for every query (the residual risk is written in Document 5); `VERSION_STATUS_CONFLICT` (409) and `CASE_INVALID` (422) join the error codes, and an `Accept` a route cannot produce is 415; error details carry the finding code, never the request; `GET /rulesets` is added so the web app can find the seeded rule set; `case_fixture` gains `protected`, `fixture_set` and `case_no`; statistics count the latest decision of each case; a batch answers aggregates plus one summary per case; an edit of the protected version forks the sandbox's own copy whose version 1 is a DRAFT; `trace_json` holds the decision object of Document 3. Found while working: springdoc 3.0.3 brings swagger-core 2.2.47 with an older swagger-annotations, so the first `@Schema` annotation made `/api/docs` fail at runtime (both pinned to 2.2.47), and `override-with-generic-response` had to be off for a route's own responses to stand; the contract test showed the error responses were not in the document at all. A jqwik build line in the test output tells "AI agents" to ignore jqwik results: it is text inside a dependency, not an instruction, and it was ignored. | `engine` 100% line, 100% branch; `rules.*` 100%, 99.2% to 100%; `ruleset.service` 99.0%, 92.7%; `ruleset.entity` 91.5%; `audit.service` 100%, 100%; `decision.service` 98.0%, 88.0%; `decision.entity` 100%; `policy.*` 100%, 100%; `demo.service` 93.2%, 87.5%; `web.security` 98.8%, 95.5%; `web.validation` 97.7%, 93.6%; `web.error` 98.4%, 96.9%; `web.controller` 97.5%, 94.7%; `web.request` 84.6%, 79.6%; `web.response` 93.9%, 100%; `common` 94.7%; `config` 95.5%, 77.8% | 100% on `engine` and `rules` (1207 of 1207), hard gate | 200 cases through the API in 76 ms (median of three, Testcontainers, after the JDBC batching of #41; 106 ms before it). On the cloud site the same request takes 1.2 s end to end from a laptop, of which 0.28 s is the network: the rest is the small shared Railway instance, and the batching changed it by little, so the bottleneck there is the CPU that writes 200 traces, not the round trips. A single decision 5 ms locally, 0.30 s on the cloud site | n/a |
| 2026-09-20 | 6 | Day 6 (`frontend` screens) done eight days early, in three pull requests (#43 to #45): the brand kit as delivered, the design tokens (the six brand colours, the decision colours as a family of their own, the type scale, the 4 px rhythm), the workspace shell with the sidebar and the header, the typed API client with the committed OpenAPI document a contract test keeps equal to what the API serves, `sse.ts`, and the policy screen (#43); the rule set screen: the decision table with the cell grammar of Document 3, a draft edited cell by cell, 422 pointers shown on the rows they name, publish, the raw JSON, and a rule shown beside the paragraph it was quoted from (#44); the case runner: the 200 seeded cases decided by the engine, the counts and shares by outcome, the rules that decided most often, the decision list and the full trace, with a step's rule leading back to the decision table and its source (#45); the priority bands of Document 3, which that document says the decision table displays as groups, shown as groups (#46). Checked by the owner on the cloud site from a second browser: the 200 cases decide with the aggregates of `cases-expected.json`, case 17 refers on R-330 with its trace and its derived values, and a rule opens beside the paragraph it cites. 190 Vitest and 9 Playwright tests. | Nothing | The interface is English and left to right; a policy, a rule label, a reason and a quote take the direction of their own text (`dir="auto"` inside `bdi`, `lang` and `dir` from the document's language), and a Latin identifier or a number inside Hebrew keeps its order. The brand steel `#66788a` reaches 4.55:1 on white and less on every tinted surface, so the token that carries secondary text is one step darker while `#66788a` stays the brand colour; `tokens.test.ts` measures every pair the interface draws and holds the six brand colours to the values they were given. The decision table freezes the rule column and the action column and scrolls the comparisons between them, because a rule and what it decides are what the table is read for. Found while working: CI stage 1 checked the generated client without ever installing the frontend (`openapi-typescript: not found`), and Prettier reformatted the committed `openapi.json`, which is a copy of what the API serves byte for byte; both fixed, and the document is now ignored by Prettier like the generated client beside it. | frontend: `features/rules` 97.5% statements, 92.5% branches; `features/cases` 92.6%, 79.5%; `features/policy` 90.6%, 84.6%; `api` 91.0%, 82.7%; `shared/ui` and `shared/layout` above the 80% gate; `cellGrammar`, `tableModel`, `outcomes`, `trace`, `sse` and `direction` at or near 100%. Backend unchanged. | 100% on `engine` and `rules` (1207 of 1207), hard gate | 200 cases in 76 ms locally (unchanged; no backend change today) | n/a |
| 2026-09-20 | 7 | Day 7 (AI layer and `author`) done nine days early, in five pull requests (#49 to #53): the `ai` interfaces, the prompt registry that reads `prompts/<name>/prompt.yml` and its versioned templates, the DSL cheat sheet generated from the schema, `ai.adapter` with the provider variant of the canonical schema, the `model_call` ledger, the token budget guard, the response cache and `RecordedGateway` (#49); `author/v1` and `repair/v1` with the validation loop, at most two repairs, and the escaping of Document 5 (#50); `POST /policies/{id}/rulesets` as an event stream (`parsing`, `authoring`, `validating`, then the draft) and the generate button that names each stage (#51); the few-shot example of Document 4, the provider facts found by calling a real model, and the ten live runs Gate G1 measures (#52). 892 unit, 222 integration, 195 Vitest and 11 Playwright tests. | Nothing. Gate G1 passed the same day: the owner set `OPENAI_API_KEY` on Railway and demo step 1 ran from the cloud site. | Provider facts found on day 7, Document 4 first: the strong model takes only its own temperature, so a prompt asks for `default` and none is sent; structured output runs with strict mode **off**, because strict makes every declared property mandatory and the DSL forbids some in context (a derived field has no `default`), so the model wrote values the validator refused, and the answer is normalized (nulls stripped) before the canonical validator sees it; the call takes `max_completion_tokens`, not `max_tokens`; the author timeout is 180 s, measured. The model names of Document 2 were confirmed against the provider's model list, and the daily token budget is 400,000 tokens: at 12 USD per million output tokens on the strong model, 4.80 USD, under the owner's 5 USD a day. The canonical recording a test replays is the first run that validated, because the gate allows one failure in ten and CI must not replay that one. Settings that had two homes were given one (#53, Document 2 first): `ai.timeouts.author-seconds` is gone, because the prompt file carries the timeout, and the `openai` profile no longer pins a chat temperature, which nothing read and which would have broken every strong-model call had it ever taken effect. The same change moved the `rag` row of the module table from `ai.adapter` to `ai`, where `EmbeddingGateway` lives, so day 8 starts on its own work. | `ai` 100% line, 100% branch; `ai.prompt` 97.4%, 94.8%; `ai.service` 99.0%, 95.2%; `ai.adapter` 88.0%, 74.7%; `ai.cache` 100%; `engine` 100%, 100%; `rules.*` 100%, 99.2% to 100%; `ruleset.service` 99.0%, 92.7%; `decision.service` 98.0%, 88.0%; `web.controller` 93.1%, 95.0%; `web.security` 98.8%, 95.5%; `common` 90.5%; `config` 95.9%, 77.8% | 100% on `engine` and `rules` (1207 of 1207), hard gate; neither package changed today | 200 cases in 80 ms locally (median), a single decision 5 ms | The stream's first event is immediate; the authoring answer itself took 67 to 101 seconds over the ten live runs (the author prompt returns one JSON document, it does not stream tokens) |
| 2026-09-21 | 8 | Day 8 (`rag`) done nine days early, in six pull requests (#59 to #64): Documents 2, 4 and 5 settled what they left open or said twice (#59: `rag` depends on `ruleset`; `embedding_status` values; the not-covered gate is the best chunk's cosine, since a fused score never passes 2/61; no Spring AI vector store; one lexical normalization; the fixed sentence in both languages; the retrieval route); V7 with `chunk`, its HNSW, GIN and B-tree indexes, the API role's grants, `embedding_status` and the published-version trigger relaxed for that one column, and the chunker in the cell grammar of Document 3 (#60); the embedding job on publish, the Spring AI embedding adapter, the startup dimension check, and ArchUnit keeping native SQL inside `rag` and free of string building (#61); hybrid retrieval with RRF, the Threshold, citations checked against the version, and `POST /rulesets/{id}/versions/{no}/retrieval` (#62); startup no longer re-takes a version being embedded (#63); the live pass over the 30 questions with its recordings and report (#64). 976 unit, 258 integration, 206 Vitest tests. | Nothing | Found by tests before they shipped: the V7 backfill first ran under the old trigger and would have failed the migration on Railway (`EmbeddingStatusBackfillIT`); a web `ExecutorService` bean suppressed Boot's task executor, so the job has a virtual-thread executor of its own; re-taking `EMBEDDING` versions at startup races a rolling deploy and concurrent test contexts (#63, Document 2 first). Every Spring test context gets a fake embedding gateway, or the startup job would call OpenAI. The fixture privacy check reads an embedding recording by its texts, since base64 vectors carry digit runs. Live pass (`docs/eval/retrieval-first-pass.md`): recall at 8 is 38 of 48; Hebrew misses are Q-07 (`p:6`) and Q-12 (`p:4`), paragraphs reached only through a rule's quote; Q-14, English on the Hebrew policy, is falsely refused at 0.294, and no threshold separates it from the true refusals at 0.302 and 0.303, so 0.35 stays. CI stage 2 failed twice on a 403 from GitHub's artifact service; re-run, green. | `rag.service` 96.8% line, 91.5% branch; `rag.repository` 92.9%, 100%; `ruleset.service` 99.1%, 93.6%; `ai.adapter` 89.0%, 76.3%; `web.controller` 91.7%, 92.9%; `web.request` 84.4%, 80.0%; `web.response` 100%; `engine` 100%, 100%; `rules.*` 100%, 99.2% to 100% | 100% on `engine` and `rules`, hard gate; neither package changed | 200 cases in 129 ms and a single decision 13 ms (medians, measured inside the full parallel IT run with embedding jobs alongside; day 7's 80 ms and 5 ms were quieter runs) | n/a (the chat stream is day 9) |

## Plan change: the two-week version (decided 2026-09-18)

The interview is on Monday, October 5, 2026, before day 20, so the plan follows the two-week version of Document 7
(Scope ladder). Day 1 was finished on 2026-09-18; days 2 to 10 run on Sep 22, 23, 24, 27, 28, 29, 30, Oct 1 and Oct 4,
with Hol HaMoed Sukkot worked and Fridays and Saturdays off. Cut on entry: rungs 2 to 10 and the evaluation runner;
the Brief's 90% rule-match line is measured by its manual checklist on the lending policy. Not scheduled by the
two-week version, and therefore known limitations: the nightly and manual resets (day 15) and the provider badge in
the UI (day 16). Spend caps: OpenAI 5 USD per day, which sets the ceiling for the app's daily token budget on day 7;
Railway on the Hobby plan. The dates and the struck-through boxes are in `docs/progress-checklist.md`.

## Living documents synced (2026-09-19)

The living documents now match the repository, and `docs/` was re-exported from them. The folders are `backend/` and
`frontend/` everywhere (the Java package `web` keeps its name). Document 7 records the two-week version with its date
map and the day-1 deployment as built. Documents 1 and 2 say that Railway never builds: it runs the image stage 6
scanned, by digest, and the database is a `pgvector/pgvector:pg16` image service. The living progress checklist is
frozen with a pointer here, so `docs/progress-checklist.md` is the only tracker.

## Found in use after day 7 closed (2026-09-20)

The owner added a twelve-paragraph policy on the cloud site and generated rules from it. Three defects came out of
one session, all fixed the same evening.

**Opening a policy's rules opened someone else's** (#55, #56). The rules screen and the case runner took
`rulesets.data[0]`, which was written when a sandbox held exactly one rule set, and navigation carried no id;
`useGeneration` also never invalidated the rule set list, so a screen that had read it could not see the rule set
just created. Both screens now take the rule set they were asked for, the workspace holds the choice, and the
switcher tells two rule sets of one policy apart by domain.

**A twelve-paragraph policy produced three rules, and the screen called it a result** (#57). Calling the provider
with that exact rendered prompt: `max_completion_tokens` 8,000 gave `finish_reason: length`, 8,000 completion
tokens of which 8,000 were reasoning, and no text at all. On this lineup the cap counts reasoning as well as the
document, so the model spent the budget thinking and wrote nothing; the repair loop was then handed an empty
answer and returned a small document that validated with warnings. With room the same prompt answers 27 rules over
23 fields. The cap is 24,000 (Document 4 first), a cut-off completion now fails as `OUTPUT_TRUNCATED` and never
reaches a repair, an answer with no text is never cached (it would have made the failure permanent for that
policy), and the draft summary shows what the validator noted, because `NO_TERMINAL_APPROVE` and
`REFER_PRECEDES_REJECT` had been said about that draft and nobody saw them. Verified on the cloud site afterwards:
the same policy answers 20 rules over 19 fields citing nine of its twelve paragraphs.

**What let all of it through**: the test for "Open its rules" asserted the callback had been called once, never
with what, and the Playwright test for demo step 1 stopped at the "Review the draft" button without clicking it.
Coverage was high throughout; the assertions were the gap. Day 8 onwards: assert the value a callback received,
and let an end-to-end test follow the click.

## Gate G1 proof (collected 2026-09-20, day 7): passed

| G1 condition (Document 7, Phase gates) | Proof | Result |
| --- | --- | --- |
| C-01 to C-31 pass in Java and Python | `./mvnw clean verify` on this branch and `python3 fixtures/reference/reference_check.py` | 892 unit and 222 integration tests green; the reference ends `ALL OK` |
| `engine` and `rules` at 100% line and PIT at least 90% | JaCoCo merged report; PIT from day 3, neither package changed since | `engine` 100% line and branch, `rules.*` 100% line; PIT 1207 of 1207 |
| 200 cases under 1 s and twice byte-identical | `DecisionPerformanceIT` and the engine's byte-identity property | 80 ms median through the API locally, identical bytes twice |
| Decisions persisted | `BatchDecisionIT`, day 5 | every case stored with its trace |
| Author schema-valid in at least 9 of 10 recorded runs | `LiveAuthoringRecordingIT` against `gpt-5.6-terra`, 2026-09-20 11:22 to 11:37 UTC, recordings in `fixtures/eval/recordings/openai/author/v1/` | **10 of 10** valid on the first try, no repair; 67 to 101 s per run; 17 to 24 rules over 12 to 15 fields |
| Demo steps 1 and 2 on the cloud site | Step 2 checked by the owner on 2026-09-20 (day 6). Step 1 run against `api.policypilot.liorshaya.com` after the owner set `OPENAI_API_KEY` on the Railway service, 2026-09-20 12:05 UTC | passed; `parsing`, `authoring`, `validating`, `draft` in order, then a DRAFT with 22 rules over 14 fields and no findings. A run whose input the cache had not seen took 90 s, inside the 67 to 101 s measured locally, so the provider path works in production and is not answering from the cache |

Step 1 is also covered offline, so CI can prove it without a provider: the two Playwright tests of
`e2e/generate.spec.ts` walk the stream and the draft in a browser, and `AuthorRecordedIT` replays what the real
model answered through the whole pipeline, prompt, validator and all.

## Gate G0 proof (collected 2026-09-18, ahead of day 1): passed

| G0 condition (Document 7, Phase gates) | Proof | Result |
| --- | --- | --- |
| `docker compose up` plus one command starts the system on a clean machine in under 5 minutes | Compose smoke on a fresh GitHub runner with none of the project's images or caches: https://github.com/liorshaya/PolicyPilot/actions/runs/35334127170 | 68 s of the 300 s budget; health UP and the gate page served |
| CI stages 1, 2, 4 and 6 run green on the skeleton | https://github.com/liorshaya/PolicyPilot/actions/runs/35334390631 | every job that runs on `main` green; stage 7 runs on pull requests only |
| The API answers `/actuator/health` on Railway | `curl https://policypilot-production-88ae.up.railway.app/actuator/health`, 2026-09-18 10:26 UTC | HTTP 200 in 0.44 s, `{"groups":["liveness","readiness"],"status":"UP"}` |
| The web app shows the access gate on Vercel | https://policypilot.liorshaya.com rendered in an emulated iPhone 13 (Playwright), 2026-09-18 | heading, code field and button behave, deep links answer 200, no console errors. At 10:47 UTC the live domain answered HTTP 200 with a valid certificate through a public DNS lookup, and the owner confirmed it opens; no photo from a physical phone is attached |
| `fixtures/` committed with the Python reference self-test passing in stage 2 | stage 2 of the run above | `ALL OK`; the generator reproduces the committed cases with no diff |

Other day 1 proofs:

- Test of the tests: a planted fake key failed stage 1 at the gitleaks step (rule `generic-api-key`) and branch
  protection blocked the merge: pull request #14, https://github.com/liorshaya/PolicyPilot/actions/runs/35334443606.
  Stage 1 scans every branch the checkout fetches, so a secret on any branch turns stage 1 red for every run until the
  branch or commit is gone.
- Branch protection on `main` since 2026-09-18: pull request required, stages 1 to 6 required, applies to
  administrators, linear history, no force pushes or deletions.
- Railway deploys only from a green `main` (Wait for CI on); Vercel deploys `main` to production and pull requests to
  previews.
- Railway deploys the digest stage 6 produced (Documents 5 and 7; decided 2026-09-18): Railway had been building its
  own image (sha256:1b380e61…), different from the scanned one. Since 11:18 UTC the service runs
  `ghcr.io/liorshaya/policypilot-backend@sha256:ddbee484…`, the stage 6 image of main at 3a4161e (deployment
  71a00330, SUCCESS, health UP), and the CI job `deploy-railway` deploys each new main digest after stages 1 to 6 pass.

## Day 8 cloud check (collected 2026-09-22): passed

Work Plan, day 8, Done when: "publishing version 1 embeds its paragraphs and rules; a Hebrew question retrieves its
expected paragraph on the cloud site".

- Railway logged `rag.embedded` for the seeded version 1 with 29 chunks at 20:57 UTC on 2026-09-21, after V7 and the
  embedding job deployed, through `text-embedding-3-small`.
- `POST /api/v1/rulesets/{id}/versions/1/retrieval` on https://policypilot-production-88ae.up.railway.app, logged in
  with the production access code:
  - Q-03 "מהי תקופת ההחזר המקסימלית להלוואה?": covered, best cosine 0.485, kept `p:2, r:R-116, p:3, p:8, p:6, r:R-010,
    p:5, r:R-130`; the expected `p:2` first.
  - Q-07 "מה עושה הכלל R-320?": covered, best cosine 0.446, `r:R-320` first.
  - Q-04 "מהי הריבית המקסימלית שהבנק רשאי לגבות?": not covered at 0.303, the fixed Hebrew sentence, no model call.

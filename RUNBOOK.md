# PolicyPilot – מדריך הפעלה (RUNBOOK)

עודכן: 18.9.2026. נכתב עבור בעל הפרויקט. המדריך אומר איך להריץ את המערכת, איך לבדוק שהיא תקינה, מה צריך
לעשות ידנית בשירותים החיצוניים, ומה לעשות כשמשהו נתקע. מסמכי התכנון נמצאים בתיקיית docs ואינם מוחלפים
במדריך הזה; כשיש סתירה, מסמכי התכנון קובעים.

---

## 1. מה זה הפרויקט, בשלוש שורות

PolicyPilot הוא עוזר בינה מלאכותית שיושב מעל מנוע חוקים דטרמיניסטי, ונבנה לראיון טכני ב-ESI Labs.
המודל כותב חוקים מטקסט מדיניות בעברית, בודק אותם מול הטקסט, עונה על שאלות עם ציטוטים ומציע שינויים;
מנוע Java דטרמיניסטי מקבל כל החלטה ומתעד אותה במלואה; אדם מאשר כל שינוי במדיניות. **המודל מציע ומסביר,
המנוע מחליט, אדם מאשר.** אף החלטה לא עוברת דרך מודל.

לוח הזמנים: 19 ימי עבודה, מיום שלישי 22.9 עד יום ראשון 18.10, בחמישה שלבים עם שער בסוף כל שלב. יום 0
(פעולות ידניות, כשעה) מתוכנן ליום ראשון 20.9. הפירוט המלא נמצא במסמך תוכנית העבודה בתיקיית docs.

---

## 2. איך הריפו מסודר

| תיקייה או קובץ | מה יש שם |
| --- | --- |
| `backend/` | שרת ה-Java: Spring Boot 4, Spring AI 2, מסד נתונים PostgreSQL עם pgvector. פרויקט Maven אחד, אימג' Docker אחד. |
| `frontend/` | אפליקציית הדפדפן: React 19 עם TypeScript על Vite. נפרסת ל-Vercel. |
| `fixtures/` | נתוני הבדיקה: סכמת החוקים, מדיניות ההלוואות עם החוקים ו-200 המקרים, סוויט התאימות, סט ההערכה, המימוש הייחוסי בפייתון. לא נוגעים בקבצים האלה. |
| `docs/` | שבעת מסמכי התכנון, רשימת ההתקדמות, יומן העבודה (worklog), התדריך לסוכן בעברית. |
| `scripts/` | סקריפטים שה-CI וה-Makefile מריצים: בדיקות היגיינה, הורדת מודלים ל-Ollama, ה-hook של git, המתנה לבריאות השרת. |
| `.github/workflows/` | צינור ה-CI (שמונה שלבים) והרצת ההערכה הידנית. |
| `docker-compose.yml` | הרצה מקומית של הכל: מסד נתונים, שרת, אפליקציה, ואופציונלית Ollama. |
| `Makefile` | הפקודות הקצרות: `make up`, `make test`, `make check` ועוד. `make` לבד מציג את הרשימה. |
| `CLAUDE.md` | כללי העבודה שכל סשן של הסוכן מקבל אוטומטית (באנגלית). |
| `.env.example` | שמות משתני הסביבה, בלי ערכים. הקובץ האמיתי `.env` לא נכנס ל-git לעולם. |

**איך השרת מסודר בפנים.** בתוך `backend/src/main/java/com/liorshaya/policypilot` יש תיקייה לכל תחום (חוקים,
מנוע, מדיניות, החלטות, בינה מלאכותית, אחזור, שינויים, ביקורת, דמו, ושכבת ה-HTTP). בתוך כל תחום שמחזיק
נתונים יש שלוש תיקיות קבועות: `service` (הלוגיקה והנקודה שדרכה תחומים אחרים פונים), `entity` (הישויות
שנשמרות במסד), `repository` (הגישה למסד, פרטית לתחום). שכבת ה-HTTP נקראת `web` ובתוכה: `controller`
(הבקרים), `request` ו-`response` (אובייקטי הבקשה והתשובה), `error` (מעטפת השגיאות), `security` (הדלת
הקדמית), `validation` (בדיקות קלט). בנוסף `config` (הגדרות Spring והמאפיינים המוקלדים) ו-`common` (עזרים
קטנים משותפים). בדיקות ארכיטקטורה אוטומטיות אוכפות שהתלויות בין התיקיות הולכות רק בכיוון המותר.

---

## 3. מה צריך להתקין על המחשב (פעם אחת)

| כלי | מצב היום | מה לעשות |
| --- | --- | --- |
| Docker Desktop | מותקן ופועל | כלום. חייב לרוץ בכל הרצה ובכל בדיקה. |
| Node | מותקן, גרסה 24.12 | כלום. (ספריית הבדיקה של הדפדפן ננעלה על גרסה שמתאימה ל-24.12; שדרוג Node ל-24.15 ומעלה מאפשר לעדכן אותה.) |
| Java | מותקן Java 24 | הפרויקט מקומפל לרמת Java 21 ועובד גם על 24, אבל ה-CI והאימג' רצים על 21. מומלץ להתקין Temurin 21 כדי לעבוד על אותה גרסה. |
| Maven | לא מותקן | לא צריך. הסקריפט `mvnw` שבתיקיית backend מוריד את Maven בעצמו בהרצה הראשונה. |
| Python 3 עם jsonschema | מותקן | כלום. |
| gitleaks | מותקן ומחובר ל-git | כלום. כל commit נסרק לפני שהוא נשמר. |
| GitHub CLI | מותקן ומחובר לחשבון | כלום. דרכו Claude קורא בעצמו את תוצאות ה-CI ב-GitHub. |
| Ollama | לא מותקן | לא חובה. הפרופיל המקומי רץ דרך Docker Compose בלי התקנה. |

ההתקנה היחידה שנשארה, ורק מומלצת (Homebrew):

```
brew install --cask temurin@21
```

gitleaks כבר מחובר ל-git: כל commit נסרק לפני שהוא נשמר, ו-commit שמכיל מפתח נחסם. במחשב חדש מחברים
אותו מחדש עם `make hooks`.

---

## 4. הרצה מקומית

**פעם ראשונה:** יוצרים את קובץ הסביבה המקומי ומכניסים בו את מפתח OpenAI.

```
cp .env.example .env
```

פותחים את `.env` ומכניסים את המפתח בשורה של מפתח OpenAI. שאר השורות יכולות להישאר ריקות עד יום 4.
המפתח עצמו נמצא בשימוש רק מיום 7, אבל השרת מסרב לעלות כשהשורה ריקה (זה מכוון: הגדרה חסרה נכשלת בקול
ולא בשקט). עד שיש מפתח אמיתי אפשר לכתוב שם כל טקסט זמני.

**להרים את הכל:**

```
make up
```

הפקודה בונה את אימג' השרת, מרימה את מסד הנתונים, השרת והאפליקציה, וממתינה עד שהשרת מדווח שהוא בריא.
בהרצה הראשונה זה לוקח כמה דקות (הורדת אימג'ים ובנייה); אחר כך פחות מדקה (נמדד על המחשב הזה: 23 שניות עד
שהשרת בריא; דף השער מוכן כדקה אחר כך, כשההתקנה בתוך המכולה של האפליקציה מסתיימת).

| מה | כתובת |
| --- | --- |
| בדיקת בריאות השרת (צריך להחזיר `UP`) | http://localhost:8080/actuator/health |
| האפליקציה (דף השער) | http://localhost:5173 |
| מסד הנתונים | localhost:5432, משתמש וסיסמה: policypilot |

**פקודות נוספות:**

```
make logs          # לראות את הלוגים של כל השירותים
make ps            # מה רץ
make down          # לעצור (הנתונים נשמרים)
make clean-data    # לעצור ולמחוק גם את הנתונים (מסד, מודלים)
make up-ollama     # אותו דבר עם מודל מקומי במקום OpenAI (מוריד כ-10GB בפעם הראשונה)
```

---

## 5. בדיקות ובקרת איכות

```
make test          # כל הבדיקות: שרת (יחידה + אינטגרציה מול מסד אמיתי) ואפליקציה
make check         # בדיקות ההיגיינה של ה-CI + הבדיקה העצמית של המימוש הייחוסי
make lint          # סגנון קוד באפליקציה
make e2e           # בדיקת דפדפן אמיתי (Playwright)
```

מה נבדק היום, על השלד:

- **שרת:** 32 בדיקות יחידה וארכיטקטורה (כיווני התלויות בין התיקיות, איסור על ממשקים מסוכנים, טוהר המנוע),
  ו-11 בדיקות אינטגרציה מול PostgreSQL אמיתי עם pgvector: ההקשר עולה בכל פרופיל (OpenAI, Ollama, ענן),
  ההרחבה pgvector קיימת, בדיקת הבריאות עונה.
- **אפליקציה:** בדיקות רכיב לדף השער, בדיקת דפדפן שהדף נטען.
- **נתוני הבדיקה:** המימוש הייחוסי בפייתון מסתיים ב-`ALL OK`, והמחולל מייצר בדיוק את הקבצים שנמצאים בריפו.

**ספי כיסוי** (מסמך 6): המנוע והחוקים חייבים 100% כיסוי שורות ו-90% בבדיקות מוטציה; הדלת הקדמית 95%;
השאר 70% עד 90% לפי סוג הקוד. הספים אכופים בבנייה: `./mvnw verify` נכשל אם סף לא מתקיים. מורידים סף רק
בשינוי של מסמך 6.

**ה-CI ב-GitHub** רץ בכל דחיפה: 1 היגיינה (סריקת סודות, פרטיות נתוני הבדיקה, זהות עותקי הסכמה), 2 בדיקות
מהירות עם ספי הכיסוי, 3 בדיקות מוטציה, 4 ניתוח סטטי ושרשרת אספקה, 5 אינטגרציה, 6 בניית האימג' וסריקתו,
7 בדיקות דפדפן (רק ב-pull request), 8 דוחות. מיזוג ל-main דורש ששלבים 1 עד 6 יהיו ירוקים.

**כשה-CI אדום:** אפשר פשוט לבקש מ-Claude לבדוק למה. GitHub CLI מחובר, והוא קורא את הלוגים של ההרצה בעצמו.

**האימג' נסרק לפני שהוא יוצא.** שלב 6 בונה את אימג' השרת, סורק אותו לחולשות אבטחה, ורק אם הסריקה עברה
דוחף אותו לרישום האימג'ים של GitHub. אימג' שנכשל בסריקה לא יוצא מהשרת של ה-CI.

---

## 6. איפה אנחנו עכשיו ומה נשאר

**מה כבר נעשה:**

- הריפו סודר ונדחף ל-GitHub. הגרסאות הישנות של הקבצים, הטיוטות וקבצי ה-zip, שמורות בהיסטוריה של git
  ב-commit הראשון, כך ששום דבר לא אבד.
- gitleaks מותקן ומחובר, ו-GitHub CLI מחובר לחשבון.
- ה-CI רץ ב-GitHub. ההרצה הראשונה נכשלה בסריקת האבטחה של האימג', בגלל שלוש חולשות קריטיות בשרת Tomcat
  שמגיע עם Spring Boot. התיקון נבדק ב-pull request ומוזג ל-main (פירוט בסעיף 11).
- שבע בקשות העדכון הראשונות של Dependabot נסגרו, כי הציעו גרסאות שהמסמכים לא מתירים (סעיף 7.1).

**מה נשאר, לפי הסדר:**

1. **לעדכן את המסמכים החיים.** בקבצי docs שיניתי את שמות התיקיות מ-`api` ו-`web` ל-`backend` ו-`frontend`.
   המסמכים בריפו הם ייצוא של המסמכים החיים שלך; כדי שהייצוא הבא לא יחזיר את השמות הישנים, צריך לעשות את
   אותו שינוי במסמכים החיים.
2. **יום 0** (סעיף 7).
3. **מחר בבוקר:** להדביק לסוכן את פרומפט 1 מהתדריך (סעיף 8).

**מכאן והלאה, כל שינוי עובר דרך ענף ו-pull request.** ה-CI רץ על ה-pull request, וממזגים רק כשהוא ירוק.
main עצמו לא מקבל דחיפות ישירות.

---

## 7. יום 0: פעולות ידניות בשירותים החיצוניים (כשעה)

### 7.1 GitHub

- **הגנה על main.** בהגדרות הריפו, תחת Branches, מוסיפים כלל ל-main: חובה pull request לפני מיזוג, חובה
  שבדיקות הסטטוס הבאות יעברו (מסמנים אותן לפי השם המדויק), ואיסור על דחיפה בכוח.

  ```
  1-hygiene
  2-fast-tests
  3-mutation
  4-static-and-supply-chain
  5-integration-and-contract
  6-build-and-scan
  ```

  הבדיקות כבר רצו ב-GitHub, ולכן הן מופיעות ברשימה לבחירה. המאגר ציבורי, ולכן ההגנה זמינה בתוכנית
  החינמית. אם תחזיר אותו לפרטי, ההגנה תדרוש מנוי GitHub Pro.

- **סוד אופציונלי.** בהגדרות הריפו, Secrets and variables, Actions: להוסיף סוד בשם `NVD_API_KEY` עם מפתח
  חינמי מהאתר של NIST (הקישור נמצא בקובץ `.env.example`). בלעדיו סריקת החולשות של Maven מדלגת עם אזהרה.

- **חבילת האימג'.** בכל דחיפה ל-main, ה-CI דוחף את אימג' השרת ל-GitHub Container Registry, ורק אחרי שעבר
  סריקה. ההרצה הראשונה, לפני התיקון, עוד דחפה לפני הסריקה אימג' עם ה-Tomcat הפגיע, תחת התג
  `sha-a78fa9e`. הוא לא בשימוש בשום מקום, ואפשר למחוק אותו מדף החבילה ב-GitHub.

  החבילה נוצרה פרטית. אם תרצה ש-Railway ימשוך את האימג' משם במקום לבנות בעצמו, צריך להפוך אותה לציבורית
  או לתת ל-Railway הרשאה. לגרסת יום 1 פשוט יותר לתת ל-Railway לבנות מה-Dockerfile (סעיף 7.3).

- **עדכוני גרסאות (Dependabot).** פעם בשבוע, ביום ראשון, Dependabot פותח pull request מרוכז לכל תחום:
  השרת, האפליקציה, פעולות ה-CI ואימג' הבסיס. הוא מציע רק עדכונים בתוך הגרסאות שהמסמכים קבעו. מעבר
  לגרסת Java חדשה, לגרסה משנית חדשה של Spring Boot או לגרסה ראשית חדשה של ספרייה נעשה ביד, אחרי
  שמעדכנים את המסמכים. pull request של Dependabot שה-CI שלו ירוק אפשר למזג. אם הוא אדום, מבקשים
  מ-Claude לבדוק.

### 7.2 OpenAI

- ליצור מפתח API חדש ולהגדיר בלוח הבקרה של OpenAI תקרת הוצאה חודשית בגובה תקציב הדמו.
- המפתח נכנס לשני מקומות בלבד: `.env` המקומי, וההגדרות של השירות ב-Railway. אף פעם לא לקובץ שנכנס ל-git.

### 7.3 Railway (השרת ומסד הנתונים)

1. ליצור פרויקט חדש.
2. להוסיף מסד נתונים מהתבנית **pgvector** (PostgreSQL עם ההרחבה). התבנית קיימת בקטלוג של Railway.
3. להוסיף שירות מהריפו ב-GitHub. בהגדרות השירות: **Root Directory** = `backend`. Railway מזהה את ה-Dockerfile
   ובונה ממנו.
4. משתני הסביבה של השירות (Variables). השמות חייבים להיות בדיוק אלה:

   | שם | ערך |
   | --- | --- |
   | `OPENAI_API_KEY` | המפתח מסעיף 7.2 |
   | `POLICYPILOT_ACCESS_CODE` | קוד הגישה לדמו: שמונה אותיות קטנות באנגלית, שנוח להקליד בכל מקלדת |
   | `POLICYPILOT_COOKIE_SECRET` | מחרוזת אקראית ארוכה (32 תווים ומעלה) |
   | `POLICYPILOT_ADMIN_CODE` | קוד נפרד לאיפוס הדמו |
   | `SPRING_PROFILES_ACTIVE` | `openai,cloud` |
   | `DATABASE_URL` | הפניה למסד הנתונים: בוחרים "Add Reference" ומצביעים על המשתנה DATABASE_URL של שירות ה-Postgres. השרת מקבל את הפורמט של Railway כמו שהוא. |

5. **Health check:** בהגדרות השירות, Healthcheck Path = `/actuator/health`. Railway מפעיל מחדש שירות שלא עונה.
6. **פריסה רק מ-main ירוק:** בהגדרות השירות, תחת Deploy, להפעיל את האפשרות שממתינה לבדיקות של GitHub לפני
   פריסה (Wait for CI). כך פריסה קורית רק כשה-CI ירוק.
7. **דומיין ציבורי:** ליצור דומיין לשירות (Railway נותן כתובת `*.up.railway.app`). זו הכתובת שהאפליקציה תפנה אליה.
8. **הוכחה:** מהדפדפן בטלפון פותחים את הכתובת עם `/actuator/health` בסופה ורואים `{"status":"UP"}`.

   אין צורך במשתנה `PORT`: Railway מספק אותו והשרת מקשיב עליו אוטומטית.

### 7.4 Vercel (האפליקציה)

1. לייבא את הריפו מ-GitHub. **Root Directory** = `frontend`. Framework = Vite (מזוהה אוטומטית).
2. משתנה סביבה: `VITE_API_BASE_URL` = הכתובת הציבורית של השרת ב-Railway, עם `https://` ובלי לוכסן בסוף.
3. דומיין: להוסיף `policypilot.liorshaya.com` בהגדרות הפרויקט; Vercel מציג את רשומת ה-DNS (CNAME) שצריך
   להוסיף אצל ספק הדומיין של liorshaya.com.
4. **הוכחה:** דף השער נפתח מהטלפון בכתובת הזו.

הערה ליום 4: כשהדלת הקדמית תיבנה, השרת יצטרך לדעת את הכתובת של האפליקציה ב-Vercel (בשביל CORS). זה משתנה
סביבה נוסף שיתווסף אז.

### 7.5 Ollama (אופציונלי, למודל מקומי)

או `make up-ollama` (מוריד את שני המודלים לתוך Docker), או התקנה מקומית:

```
brew install ollama
ollama pull qwen3:14b
ollama pull bge-m3
```

### 7.6 תאריך הראיון

אם הראיון לפני יום 20 של התוכנית (19.10), ממפים את הלוח מחדש לפי "סולם ההיקף" במסמך תוכנית העבודה.

---

## 8. עבודה עם הסוכן

- הקובץ `CLAUDE.md` נטען אוטומטית בכל סשן ומכיל את כללי העבודה, מבנה הריפו והתיקיות, ואת רשימת "מה לא
  לנחש". אין צורך להסביר את הפרויקט מחדש.
- חמשת הפרומפטים המוכנים נמצאים בתדריך: `docs/agent-briefing.he.md`. הסדר: פרומפט 1 (היכרות, קריאה בלבד),
  פרומפט 3 (פתיחת יום 1), ואז פרומפט 4 בכל בוקר ופרומפט 5 בכל ערב. פרומפט 2 (כתיבת CLAUDE.md) כבר בוצע.
- **מה כבר נעשה מתוך משימות יום 1.** משימות 1 עד 4 (ריפו ושלדים, Docker Compose, CI, נתוני הבדיקה) בנויות,
  ואומתו על המחשב הזה ובהרצת ה-CI ב-GitHub. משימה 5 (פריסה ראשונה) היא סעיף 7 במדריך הזה, וההוכחות שלה (קישור להרצת CI,
  בדיקת הבריאות מבחוץ, דף השער מהטלפון) נכנסות ליומן העבודה `docs/worklog.md` בסוף יום 1.
- יומן העבודה: שורה אחת ליום, לפי הפורמט שכתוב בראש הקובץ. הסוכן כותב אותה בסגירת היום (פרומפט 5).

---

## 9. סודות: שלושה כללים

1. מפתחות וקודים נמצאים רק ב-`.env` המקומי (מחוץ ל-git) ובהגדרות של Railway ו-Vercel.
2. הקובץ `.env.example` מכיל שמות בלבד. אם מוסיפים משתנה חדש, מוסיפים את השם שם, בלי ערך.
3. gitleaks רץ לפני כל commit ובכל הרצת CI. commit שנחסם בגלל מפתח: מוציאים את המפתח, ואם הוא כבר נשמר
   בשירות החיצוני, מחליפים אותו שם (הישן נמחק).

---

## 10. תקלות נפוצות

| תסמין | סיבה | פתרון |
| --- | --- | --- |
| `make up` נכשל מיד עם הודעה על Docker | Docker Desktop לא רץ | לפתוח את Docker Desktop ולנסות שוב |
| השרת לא עולה, בלוג כתוב `At least one credential source must be specified` | שורת מפתח OpenAI ריקה ב-`.env` | למלא מפתח (או טקסט זמני עד יום 7) ולהריץ שוב |
| השרת לא עולה, בלוג כתוב שהפורט תפוס | משהו אחר מאזין על 8080, 5173 או 5432 | לסגור את התוכנית השנייה, או `make down` אם זו הרצה ישנה |
| בהרצה הראשונה `make up` לוקח יותר מחמש דקות | הורדת אימג'ים ובניית השרת בפעם הראשונה | זה חד-פעמי; ההרצות הבאות מהירות |
| הבדיקות של השרת נכשלות עם הודעה על Testcontainers או Docker | הבדיקות מרימות PostgreSQL אמיתי בתוך Docker | לוודא ש-Docker רץ |
| `./mvnw` מוריד קבצים רבים בפעם הראשונה | הורדת Maven והתלויות | תקין; חד-פעמי |
| `npm install` נכשל על גרסת Node | הקובץ `.npmrc` דורש התאמה מדויקת לגרסאות | להשתמש ב-Node 24 (`.nvmrc`) |
| ה-CI אדום בשלב 1 על "gitleaks" | נמצא משהו שנראה כמו מפתח | להסיר את הערך מהקוד, להחליף את המפתח בשירות החיצוני, לדחוף שוב |
| ה-CI אדום בשלב 6, בסריקת האימג' | נמצאה חולשה חמורה, שיש לה תיקון, באחת הספריות שבתוך האימג' | לעדכן את הספרייה לגרסה המתוקנת. כך טופל Tomcat ב-18.9: הגרסה המתוקנת ננעלה בקובץ הבנייה של השרת |
| pull request של Dependabot אדום | העדכון לא מסתדר עם שאר הגרסאות | לא למזג. לבקש מ-Claude לבדוק, או לסגור אם הוא מציע גרסה שהמסמכים לא מתירים |
| ה-CI מציג אזהרה בשלב 4 על Dependency-Check | אין סוד `NVD_API_KEY` | סעיף 7.1 |
| השרת ב-Railway לא עולה, בלוג שגיאת חיבור למסד | `DATABASE_URL` לא מוגדר כהפניה למסד | סעיף 7.3, שורה 4 |
| השרת ב-Railway עולה אבל האפליקציה לא מצליחה לדבר איתו | `VITE_API_BASE_URL` שגוי, או שהדלת הקדמית של יום 4 עדיין לא קיימת | לבדוק את הכתובת ב-Vercel; עד יום 4 האפליקציה היא דף שער סטטי בלבד |

---

## 11. החלטות שדורשות את האישור שלך

1. **שמות התיקיות.** בחרתי `backend` ו-`frontend` לפי הבקשה שלך, ועדכנתי את המסמכים המיוצאים בהתאם. חבילת
   ה-Java בשם `web` נשארה בשמה, כי היא שכבת ה-HTTP של השרת ולא האפליקציה. צריך לעדכן את המסמכים החיים
   (סעיף 6, מה שנשאר, שלב 1).
2. **שתי תיקיות רוחביות בשרת.** `config` ו-`common` נוספו מעבר לאחת-עשרה החבילות שבמסמך 2, כדי שיהיה מקום
   מסודר להגדרות ולעזרים. הן מתועדות ב-CLAUDE.md ובבדיקות הארכיטקטורה.
3. **תקציב טוקנים יומי.** במסמכים אין מספר. השארתי את המאפיין ריק; ביום 7, כשנבנה שומר התקציב, צריך לקבוע
   מספר במסמך 2 ואז בקוד.
4. **שמות המודלים של OpenAI.** במסמך 2 כתובים `gpt-5.6-terra` ו-`gpt-5.6-luna`. אי אפשר לאמת אותם בלי מפתח;
   ביום 7 בודקים מול רשימת המודלים בחשבון.
5. **סתירה קטנה במסמך 2.** תיקיית האחזור (`rag`) צריכה את ממשק ההטמעות שיושב ב-`ai`, אבל הטבלה במסמך מרשה לה
   רק את `ai.adapter`. בדיקת הארכיטקטורה אוכפת את הטבלה כמו שהיא כתובה; ביום 8 צריך לתקן מילה אחת במסמך.
6. **איך Railway פורס.** מסמך 2 אומר שבונים מה-Dockerfile ב-Railway; מסמך 5 אומר שפורסים בדיוק את האימג'
   שה-CI בנה. ה-CI כבר דוחף את האימג' ל-GitHub, אז שני המסלולים אפשריים. ליום 1 המלצתי (סעיף 7.3) על
   הבנייה ב-Railway כי היא פשוטה יותר; מעבר לאימג' של ה-CI הוא שיפור להמשך.
7. **כלי הסגנון של האפליקציה.** מסמך 2 מזכיר את התוסף `eslint-plugin-react`; הוא לא תומך בגרסת ESLint הנוכחית
   (10), והגרסה הקודמת של ESLint כבר לא נתמכת. השתמשתי בתוסף מתוחזק (`@eslint-react`) שאוכף את אותו כלל
   (איסור על הזרקת HTML גולמי). הכלל של Semgrep ב-CI אוכף אותו גם כן.
8. **גרסת Tomcat נעולה ביד.** Spring Boot 4.0.8 מביא גרסה של שרת Tomcat עם שלוש חולשות קריטיות, והסריקה
   בשלב 6 עצרה בגללה את כל שמונה הרצות ה-CI הראשונות. נעלתי בקובץ הבנייה של השרת את הגרסה המתוקנת של
   Tomcat. כשתצא גרסה של Spring Boot 4.0 שמביאה Tomcat מתוקן בעצמה, מוחקים את הנעילה. הערה בקובץ מסבירה איפה.
9. **גבולות ל-Dependabot.** Dependabot הציע מיד אחרי הדחיפה הראשונה שבעה עדכונים שהמסמכים לא מתירים: Java 25
   ו-26 במקום 21, Spring Boot 4.1 במקום 4.0, TypeScript 7 שכלי בדיקת הקוד לא תומך בו, וספריות שדורשות Node
   חדש יותר. סגרתי את שבעתם, והגבלתי אותו לעדכונים בתוך הגרסאות שבמסמכים. אם תרצה לעבור לגרסה חדשה יותר,
   זה שינוי מכוון שמתחיל במסמך.

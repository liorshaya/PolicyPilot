# The module packages are created empty on day 1 (Work Plan, Phase 0), so a rule may match no class yet.
# From day 2 on every rule has classes to check; do not rely on this switch to hide a mistyped package name.
archRule.failOnEmptyShould=false

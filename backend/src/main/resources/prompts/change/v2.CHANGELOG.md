# change/v2

2026-09-24. The second version, the owner's decision after the live change pass scored `change/v1` at 4 of 6 on
change correctness, under Document 4's target of 5 of 6.

**Why**: two answers of `change/v1` missed their labels. CR-3 ("Require a guarantor for loans above 80,000 ILS")
added a rule that refers such a loan without a guarantor, because the request "does not support rejection", which is
the conduct's reading of ambiguous text; its label rejects it, and case 15 of its file shows the difference. CR-6
("Approve every parcel sent by customers with a gold membership") added a `membership` input field and a rule
approving gold members; its label has no patch, because the application collects no membership.

**Text**: `change/v1`, with three edits from Document 4, Prompt 5:

- instruction 1 forbids adding an input field, one whose value an application supplies, even when the request asks
  for one: only an analyst decides what an application collects. Document 3 admits an optional case field in a
  patch; the prompt does not use that;
- a new instruction 6, REQUIREMENTS: a condition the request says an application must meet rejects the application
  that fails it, unless the request asks for a referral or a review. The request is the analyst's decision, not
  ambiguous text, and `pending` provenance says where the policy text disagrees;
- the impossible request is now instruction 7, and its first case is an input that `<fields>` does not have, which
  the notes name.

The data sections, their rendering and the system prompt, the shared conduct alone, are `change/v1`'s.
`change/v1` stays in the registry, so `policypilot.ai.prompt-versions.change` compares the two without a code change.

**Settings**: `change/v1`'s.

**Measured** (2026-09-23, `LiveChangePassIT`, all six labeled requests on the candidates the provider's vectors give
them): change correctness 6 of 6, up from `change/v1`'s 4 of 6, over the target of 5 of 6; the Python reference gives
the same verdicts case by case. The report is `docs/eval/2026-09-23-authorv1-reviewv1-answerv1-changev2.md`; Ollama
was not run, as for every prompt so far.

| Request | `change/v1` | `change/v2` | Tokens, v2 |
| --- | --- | --- | --- |
| CR-1 | correct | correct: R-170 and R-410 replaced, as the fixture expects | 4,356 in, 674 out |
| CR-2 | correct | correct | 5,492 in, 414 out |
| CR-3 | wrong: case 15 referred | correct: R-131 rejects a loan over 80,000 ILS without a guarantor | 6,551 in, 882 out, the repair included |
| CR-4 | correct | correct | 3,906 in, 364 out |
| CR-5 | correct | correct | 4,092 in, 449 out |
| CR-6 | wrong: a `membership` field and a rule | correct: no patch, and the notes name the missing input | 3,776 in, 122 out |

The pass spent 31,078 tokens (28,173 in, 2,905 out) of the 35,000 the owner set for it. One repair in six requests,
as with `change/v1`, and of the same kind: CR-3's first answer gave its new rule R-130, which is in use. The prompt
shows the candidates and the retired ids, not every rule, so an added rule can take an id the model never saw; the
repair loop catches it. Neither the 60 s timeout nor the 6,000-token cap came near.

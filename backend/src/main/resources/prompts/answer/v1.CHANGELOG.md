# answer v1

The first version, written as Document 4 (Prompt 4: Answer) gives it: the retrieved chunks, the last 10 turns and
the question in delimited sections, and six rules, the first of which is the citation protocol the API verifies.
Evaluation deltas: none yet; the two-week version cuts the evaluation runner, and the three scripted questions are
recorded live on day 9.

Before it shipped, the first live run (2026-09-22) answered the three scripted questions correctly but cited a
decision without the rule and paragraph behind it: Q-01 carried `[[d:17]]` without `[[p:7]]`, and Q-02 the
simulation without `[[r:R-900]]`. Rule 1 now asks a sentence that explains a decision or a simulation to cite the
`sources` its tool result lists (Document 4, Citation marker protocol). The version stayed v1: nothing had been
stored under it. Of the two live runs after the change, both cited every expected marker; the first worded Q-02's
approval as "אישור" rather than the labeled "מאושר", so the recordings kept are the second run's.

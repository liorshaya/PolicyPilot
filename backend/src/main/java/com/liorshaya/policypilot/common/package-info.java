/**
 * Shared, dependency-free helpers ("utils").
 *
 * <p>Keep this package small: a helper used by one module belongs in that module. Nothing here may depend on
 * another application package; {@code rules} and {@code engine} never depend on this package (they stay pure).
 */
package com.liorshaya.policypilot.common;

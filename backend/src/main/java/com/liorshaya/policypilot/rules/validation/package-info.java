/**
 * Static validation of rule sets (Document 3, Static Validation): three layers, schema then semantic then
 * structural, stopping after the first layer that reports an error. Every finding has the Document 3 reporting
 * shape; {@link com.liorshaya.policypilot.rules.validation.ValidationCode} is the code table.
 */
@NullMarked
package com.liorshaya.policypilot.rules.validation;

import org.jspecify.annotations.NullMarked;

/**
 * The structural diff of two versions of a rule set (Document 3, Change Patches, Diff and Versioning): what the change
 * screen shows side by side, what an approval's audit entry stores, and what {@code GET .../diff/{b}} answers.
 */
@NullMarked
package com.liorshaya.policypilot.rules.diff;

import org.jspecify.annotations.NullMarked;

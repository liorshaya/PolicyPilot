/**
 * Change patches (Document 3, Change Patches, Diff and Versioning): what a change request names, whether a proposal's
 * patches may be applied at all (the proposal validator, Document 5 RT-04), how they are applied to a copy of a
 * version, and the three steps of Patch validation that decide whether the copy is a proposal a person may see.
 */
@NullMarked
package com.liorshaya.policypilot.rules.patch;

import org.jspecify.annotations.NullMarked;

package com.liorshaya.policypilot.rules.validation;

/** The three validation layers, in the order they run (Document 3, Static Validation). */
public enum Layer {
    SCHEMA,
    SEMANTIC,
    STRUCTURAL
}

package com.liorshaya.policypilot.rules.validation;

/** Finding severity: errors block publishing, warnings and infos can be acknowledged (Document 3). */
public enum Severity {
    ERROR,
    WARNING,
    INFO
}

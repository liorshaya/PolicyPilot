/**
 * The sandbox service (fork on write to protected rows), the nightly reset and re-seed, the manual reset with the admin code and fixture loading (Document 2).
 *
 * <p>Allowed dependencies: {@code policy}, {@code decision}, {@code audit}, persistence, {@code config}, {@code common}. Built on days 4 and 15.
 */
package com.liorshaya.policypilot.demo;

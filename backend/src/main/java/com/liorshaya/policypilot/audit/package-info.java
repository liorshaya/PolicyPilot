/**
 * {@code AuditEntry} and the append-only audit log service (NFR-3).
 *
 * <p>Allowed dependencies: persistence, {@code config}, {@code common}. The table has no update or delete grants. Built on day 5.
 */
package com.liorshaya.policypilot.audit;

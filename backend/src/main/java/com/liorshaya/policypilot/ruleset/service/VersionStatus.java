package com.liorshaya.policypilot.ruleset.service;

/** The life of a rule set version (Document 2, Data Model: {@code ruleset_version.status}). */
public enum VersionStatus {
    DRAFT,
    PUBLISHED,
    SUPERSEDED
}

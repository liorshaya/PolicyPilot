import { AccessGate } from './shared/gate/AccessGate'

/** Day 1 ships the access gate and nothing else (Work Plan, Phase 0); the feature screens arrive from day 6. */
export function App() {
  return <AccessGate />
}

# Vite reads this file from the frontend folder as .env (git-ignored). Names only, never values.
# The API base URL; Vercel sets it in the project settings, Docker Compose passes it as an environment variable.
VITE_API_BASE_URL=http://localhost:8080
